---
- name: Install Prometheus from APT (or custom steps here)
  ansible.builtin.apt:
    name: "{{ prometheus_packages }}"
    state: present
    update_cache: yes

- name: Ensure Prometheus is running
  ansible.builtin.systemd:
    name: prometheus
    enabled: true
    state: started
