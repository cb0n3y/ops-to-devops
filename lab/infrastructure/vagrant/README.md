# Install vagrant

With vagrant installed you are able to create, start, stop and destroy with simple commands.

## Prerequisites

You will need:

- the `Vagrant CLI <a id='Vagrant-CLI'>https://developer.hashicorp.com/vagrant/tutorials/get-started/install</a>`